<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Edit Certifications</title>
  <link rel="stylesheet" href="/style.css" />
</head>
<body>
  <h1>Edit Certification for <%= Employee.EmpFirstName %> <%= Employee.EmpLastName %></h1>

  <form action="/editCertifications" method="POST">
    <input type="hidden" name="EmployeeID" value="<%= Employee.EmployeeID %>" />
    <input type="hidden" name="CertificationID" value="<%= Certification.CertificationID %>" />

    <label for="CertificationDescription">Certification Description</label>
    <input type="text" name="CertificationDescription" value="<%= CertDescription %>" disabled />

    <label for="CertGrade">Grade</label>
    <select name="CertGrade" required>
      <option value="A" <%= Certification.CertGrade === 'A' ? 'selected' : '' %>>A</option>
      <option value="B" <%= Certification.CertGrade === 'B' ? 'selected' : '' %>>B</option>
      <option value="C" <%= Certification.CertGrade === 'C' ? 'selected' : '' %>>C</option>
      <option value="D" <%= Certification.CertGrade === 'D' ? 'selected' : '' %>>D</option>
      <option value="F" <%= Certification.CertGrade === 'F' ? 'selected' : '' %>>F</option>
    </select>

    <button type="submit">Submit Changes</button>
  </form>

  <a href="/viewCertifications">Back to Certifications List</a>
</body>
</html>